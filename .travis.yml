languaje: java
jdk:
  - openjdk8

sudo: false

install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3

script:
  - xvfb-run make test
  - mvn test -B